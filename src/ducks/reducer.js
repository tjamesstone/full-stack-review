//INITIAL STATE
const initialState = {
    loggedIn: false,
    user: null
}

//ACTION CONTS


//ACTION BUILDERS

//REDUCER FUNC
const reducer = (state = intitialState, action) => {
    switch(action.type) {
        default: return state
    }
}


export default reducer